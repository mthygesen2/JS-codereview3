import { noAnswers } from 'js-codereview3/helpers/no-answers';
import { module, test } from 'qunit';

module('Unit | Helper | no answers');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = noAnswers([42]);
  assert.ok(result);
});
